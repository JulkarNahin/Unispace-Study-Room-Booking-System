RewriteEngine On

# Send all non-existing files/directories to index.php so /api/* routes work in Apache/Laragon.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
